<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tutorfinderapp">

    <uses-permission android:name="android.permission.internet" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity_splash"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="false" >
        </activity>
        <activity android:name=".TutorSearchActivity" />
        <activity android:name=".TeacherSignUp" />
        <activity android:name=".TeacherProfileActivity" />
        <activity android:name=".StudentToTeacherProfileActivity" />
        <activity android:name=".StudentSignUpActivity" />
        <activity android:name=".StudentProfileActivity" />

    </application>

</manifest>